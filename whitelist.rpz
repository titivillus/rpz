; Data i hora:   16/10/2024 07:23:45
; Registres:     0
$TTL 3600      ; Temps de vida del registre (en segons)
@ SOA localhost. root.localhost. (
                 2024101601 ; Serial
                 86400      ; Refrescar cada 24 hores
                 7200       ; Temps de repetició (retry interval)
                 172800     ; Expiració en 2 dies (expiry interval)
                 3600       ; Mínim TTL (minimum TTL)
                 )
  NS localhost.

